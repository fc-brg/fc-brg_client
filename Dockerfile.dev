FROM node:20-alpine

WORKDIR /app

# 패키지 파일 복사
COPY package*.json ./

# 개발 의존성 설치
RUN npm install

# 소스 코드 복사
COPY . .

# 포트 노출
EXPOSE 5173

# 개발 서버 실행 (호스트 접근 허용, 5173 포트 사용)
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "5173"]